# Environment and secrets
.env
.env.*
# .env.* above matches .env.example, which is documentation and must ship: the
# setup steps in README.md and CLAUDE.md both start with `cp .env.example .env`,
# and without this negation a fresh clone has nothing to copy.
!.env.example
*.pem
*.key
gcp-sa-key.json
.agent_engine_resource
# ADK's local session store, created by `adk web` (make dev). It records every
# conversation event, including tool arguments and the data tools returned.
.adk/

# Python
__pycache__/
*.py[codz]
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv
venv/
env/

# uv
# uv.lock is committed — do not add it here

# Testing and coverage
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.hypothesis/

# Type checkers
.mypy_cache/
.dmypy.json
.pyre/
.pytype/

# Ruff
.ruff_cache/

# Pre-commit
.pre-commit-cache/

# Node (promptfoo)
node_modules/
output.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# macOS
.DS_Store

# Temporary files
*.tmp
*.bak
